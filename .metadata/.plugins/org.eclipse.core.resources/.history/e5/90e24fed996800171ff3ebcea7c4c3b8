/**
 * Generated from platform:/resource/ca.mcgill.sure.variablecalibration.patterns/src/ca/mcgill/sure/variablecalibration/patterns/patterns.vql
 */
package ca.mcgill.sure.variablecalibration.patterns;

import ca.mcgill.sure.variablecalibration.Configuration;
import ca.mcgill.sure.variablecalibration.Value;
import ca.mcgill.sure.variablecalibration.patterns.util.ValuesOfConfigQuerySpecification;
import java.util.Arrays;
import java.util.List;
import org.eclipse.viatra.query.runtime.api.IPatternMatch;
import org.eclipse.viatra.query.runtime.api.impl.BasePatternMatch;
import org.eclipse.viatra.query.runtime.exception.ViatraQueryException;

/**
 * Pattern-specific match representation of the ca.mcgill.sure.variablecalibration.patterns.valuesOfConfig pattern,
 * to be used in conjunction with {@link ValuesOfConfigMatcher}.
 * 
 * <p>Class fields correspond to parameters of the pattern. Fields with value null are considered unassigned.
 * Each instance is a (possibly partial) substitution of pattern parameters,
 * usable to represent a match of the pattern in the result of a query,
 * or to specify the bound (fixed) input parameters when issuing a query.
 * 
 * @see ValuesOfConfigMatcher
 * @see ValuesOfConfigProcessor
 * 
 */
@SuppressWarnings("all")
public abstract class ValuesOfConfigMatch extends BasePatternMatch {
  private Configuration fConf;
  
  private Value fValue;
  
  private static List<String> parameterNames = makeImmutableList("conf", "value");
  
  private ValuesOfConfigMatch(final Configuration pConf, final Value pValue) {
    this.fConf = pConf;
    this.fValue = pValue;
  }
  
  @Override
  public Object get(final String parameterName) {
    if ("conf".equals(parameterName)) return this.fConf;
    if ("value".equals(parameterName)) return this.fValue;
    return null;
  }
  
  public Configuration getConf() {
    return this.fConf;
  }
  
  public Value getValue() {
    return this.fValue;
  }
  
  @Override
  public boolean set(final String parameterName, final Object newValue) {
    if (!isMutable()) throw new java.lang.UnsupportedOperationException();
    if ("conf".equals(parameterName) ) {
    	this.fConf = (Configuration) newValue;
    	return true;
    }
    if ("value".equals(parameterName) ) {
    	this.fValue = (Value) newValue;
    	return true;
    }
    return false;
  }
  
  public void setConf(final Configuration pConf) {
    if (!isMutable()) throw new java.lang.UnsupportedOperationException();
    this.fConf = pConf;
  }
  
  public void setValue(final Value pValue) {
    if (!isMutable()) throw new java.lang.UnsupportedOperationException();
    this.fValue = pValue;
  }
  
  @Override
  public String patternName() {
    return "ca.mcgill.sure.variablecalibration.patterns.valuesOfConfig";
  }
  
  @Override
  public List<String> parameterNames() {
    return ValuesOfConfigMatch.parameterNames;
  }
  
  @Override
  public Object[] toArray() {
    return new Object[]{fConf, fValue};
  }
  
  @Override
  public ValuesOfConfigMatch toImmutable() {
    return isMutable() ? newMatch(fConf, fValue) : this;
  }
  
  @Override
  public String prettyPrint() {
    StringBuilder result = new StringBuilder();
    result.append("\"conf\"=" + prettyPrintValue(fConf) + ", ");
    
    result.append("\"value\"=" + prettyPrintValue(fValue)
    );
    return result.toString();
  }
  
  @Override
  public int hashCode() {
    final int prime = 31;
    int result = 1;
    result = prime * result + ((fConf == null) ? 0 : fConf.hashCode());
    result = prime * result + ((fValue == null) ? 0 : fValue.hashCode());
    return result;
  }
  
  @Override
  public boolean equals(final Object obj) {
    if (this == obj)
    	return true;
    if (!(obj instanceof ValuesOfConfigMatch)) { // this should be infrequent
    	if (obj == null) {
    		return false;
    	}
    	if (!(obj instanceof IPatternMatch)) {
    		return false;
    	}
    	IPatternMatch otherSig  = (IPatternMatch) obj;
    	if (!specification().equals(otherSig.specification()))
    		return false;
    	return Arrays.deepEquals(toArray(), otherSig.toArray());
    }
    ValuesOfConfigMatch other = (ValuesOfConfigMatch) obj;
    if (fConf == null) {if (other.fConf != null) return false;}
    else if (!fConf.equals(other.fConf)) return false;
    if (fValue == null) {if (other.fValue != null) return false;}
    else if (!fValue.equals(other.fValue)) return false;
    return true;
  }
  
  @Override
  public ValuesOfConfigQuerySpecification specification() {
    try {
    	return ValuesOfConfigQuerySpecification.instance();
    } catch (ViatraQueryException ex) {
     	// This cannot happen, as the match object can only be instantiated if the query specification exists
     	throw new IllegalStateException (ex);
    }
  }
  
  /**
   * Returns an empty, mutable match.
   * Fields of the mutable match can be filled to create a partial match, usable as matcher input.
   * 
   * @return the empty match.
   * 
   */
  public static ValuesOfConfigMatch newEmptyMatch() {
    return new Mutable(null, null);
  }
  
  /**
   * Returns a mutable (partial) match.
   * Fields of the mutable match can be filled to create a partial match, usable as matcher input.
   * 
   * @param pConf the fixed value of pattern parameter conf, or null if not bound.
   * @param pValue the fixed value of pattern parameter value, or null if not bound.
   * @return the new, mutable (partial) match object.
   * 
   */
  public static ValuesOfConfigMatch newMutableMatch(final Configuration pConf, final Value pValue) {
    return new Mutable(pConf, pValue);
  }
  
  /**
   * Returns a new (partial) match.
   * This can be used e.g. to call the matcher with a partial match.
   * <p>The returned match will be immutable. Use {@link #newEmptyMatch()} to obtain a mutable match object.
   * @param pConf the fixed value of pattern parameter conf, or null if not bound.
   * @param pValue the fixed value of pattern parameter value, or null if not bound.
   * @return the (partial) match object.
   * 
   */
  public static ValuesOfConfigMatch newMatch(final Configuration pConf, final Value pValue) {
    return new Immutable(pConf, pValue);
  }
  
  private static final class Mutable extends ValuesOfConfigMatch {
    Mutable(final Configuration pConf, final Value pValue) {
      super(pConf, pValue);
    }
    
    @Override
    public boolean isMutable() {
      return true;
    }
  }
  
  private static final class Immutable extends ValuesOfConfigMatch {
    Immutable(final Configuration pConf, final Value pValue) {
      super(pConf, pValue);
    }
    
    @Override
    public boolean isMutable() {
      return false;
    }
  }
}
